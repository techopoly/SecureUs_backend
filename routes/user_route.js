const express = require('express');

const router = express.Router();
const post_controller = require('../controller/user_controller');

// router('/login', post_controller.user_login);
// router('/signup', post_controller.user_signup);
// router('/logout', post_controller.user_logout);


module.exports = router;